# Bỏ qua thư mục node_modules (cả frontend và backend)
node_modules/
crypto-news-chart/node_modules/
server/node_modules/

# Bỏ qua các file lock của package manager (nếu không muốn đẩy lên)
yarn.lock
pnpm-lock.yaml

# Bỏ qua file môi trường chứa thông tin nhạy cảm
.env
server/.env
client/.env

# Bỏ qua thư mục build (nếu có)
build/
client/build/
server/build/

# Bỏ qua các thư mục log, cache
logs/
*.log
